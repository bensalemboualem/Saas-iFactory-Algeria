# Server
PORT=4000
NODE_ENV=development

# Auth bypass (dev only)
AUTH_BYPASS=true
AUTH_BYPASS_USER_ID=dev-user-001

# SQLite (optional)
# Par défaut, la DB est dans ./data/dzir.db
# SQLITE_PATH=./data/dzir.db

# Qdrant
QDRANT_URL=http://localhost:6333

# OpenAI (REQUIRED for embeddings/search/ask)
OPENAI_API_KEY=REDACTED_OPENAI_API_KEY
OPENAI_EMBEDDING_MODEL=text-embedding-3-small
OPENAI_API_BASE_URL=https://api.openai.com/v1/embeddings

# Credits
CREDITS_ENABLED=false
CREDITS_FAIL_OPEN=true

# Rate limit
RATE_LIMIT_ENABLED=true

# MinIO (optional - seulement si upload fichiers)
MINIO_ENDPOINT=localhost
MINIO_PORT=9000
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin
MINIO_USE_SSL=false
MINIO_BUCKET=dzir-ia
